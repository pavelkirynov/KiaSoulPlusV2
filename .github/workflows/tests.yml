name: Unit tests

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GRADLE_OPTS: -Dorg.gradle.console=plain -Dorg.gradle.daemon=false
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        # Google removed the legacy standalone "tools" package from the SDK
        # repository manifest, so the action's own default install (which
        # requests it) now fails unconditionally. An empty packages list
        # skips that broken default while still setting up ANDROID_HOME and
        # pre-accepting the SDK licenses; Gradle/AGP fetch the platform and
        # build-tools it actually needs on its own.
        with:
          packages: ""

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Run unit tests
        timeout-minutes: 20
        run: |
          ./gradlew testDebugUnitTest \
            --stacktrace \
            --console=plain \
            --no-daemon \
            -Dorg.gradle.internal.http.connectionTimeout=60000 \
            -Dorg.gradle.internal.http.socketTimeout=60000

      # The HTML report is the readable artifact; the XML is what a reader greps.
      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-report
          path: |
            app/build/reports/tests/testDebugUnitTest/
            app/build/test-results/testDebugUnitTest/
          if-no-files-found: warn
