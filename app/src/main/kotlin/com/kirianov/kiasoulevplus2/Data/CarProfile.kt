package com.kirianov.kiasoulevplus2.Data

/**
 * ЩО ЗА АВТО ПЕРЕД НАМИ.
 *
 * З'явилося тоді, коли застосунок перестав бути «моїм на моїй машині». Доти все,
 * що він знав про авто, було константами в коді: шістнадцять комірок CATL,
 * 50.88 кВт·год. Це чесно описувало одну конкретну машину — і ставало небезпечним
 * у першу ж мить, коли APK потрапляв до іншої людини. На стоковому Soul EV з
 * рідними 27 кВт·год такий застосунок упевнено обіцяв би вдвічі більший запас, і
 * людина поїхала б за цією цифрою.
 *
 * [vin] — те єдине на шині, що відповідає на питання «а це та сама машина». Без
 * нього телефон, підключившись до чужого авто, спокійно домішав би його поїздки до
 * чужої моделі й зіпсував би обидві.
 */
data class CarProfile(
    val vin: String = "",

    /** Як водій назвав авто. Порожньо — покажемо хвіст VIN. */
    val name: String = "",

    /**
     * Корисна ємність пакета, кВт·год. Нуль означає «ще не питали».
     *
     * Вивести це число з чогось на шині неможливо: BMS рахує відсотки за паспортом
     * РІДНОГО пакета й про заміну не знає — саме тому шкала на перепакованій
     * батареї й виходить нерівною. Знає про заміну лише той, хто її робив.
     */
    val packKwh: Double = 0.0,

    /** Коли це авто бачили востаннє. За ним впорядковується список. */
    val lastSeenAtMs: Long = 0L,

    /**
     * «Відбиток» авто: пробіг і пожиттєві лічильники, якими воно скінчило минулого
     * разу. Другий свідок особи, окрім VIN.
     *
     * НАВІЩО. VIN на цій машині часто мовчить (віддається лише при ввімкненому
     * запаленні, а на зарядці й поготів). З одним авто це не біда, а з кількома —
     * застосунок не знає, чиї числа, й вимикає навчання та прогноз. Відбиток закриває
     * цю дірку: якщо пробіг і лічильники плавно продовжують саме це авто (виросли, не
     * впали, і в межах правдоподібного), значить це воно — навіть коли VIN промовчав.
     *
     * Чужу батарею це не впізнає помилково: її абсолютні лічильники інші, а пробіг
     * відрізняється на десятки тисяч — неперервність не складеться. Нуль означає «ще
     * не знімали»: тоді впізнання за відбитком не працює, лишається VIN або вибір
     * руками.
     */
    val lastOdometerKm: Double = 0.0,
    val lastKwhIn: Double = 0.0,
    val lastKwhOut: Double = 0.0,
) {
    /** Чи є з чим порівнювати неперервність: без відбитка впізнати за ним не можна. */
    val hasFingerprint: Boolean get() = lastOdometerKm > 0.0 && lastKwhIn > 0.0
    /**
     * Ємність, з якою рахувати просто зараз.
     *
     * Обережність однобока навмисно: поки ємність не задано, беремо РІДНИЙ пакет, а
     * не перепакований. Занизити запас ходу означає, що людина зайвий раз
     * зарядиться; завищити — що вона стане на дорозі.
     */
    val effectivePackKwh: Double
        get() = if (packKwh > 0.0) packKwh else Pack.ORIGINAL_CAPACITY_KWH

    val packKnown: Boolean get() = packKwh > 0.0

    val known: Boolean get() = vin.isNotEmpty()
}

/**
 * Усі авто, про які застосунок щось знає, і те, за яке він рахує зараз.
 *
 * [detectedVin] — що прочитали з шини в цьому підключенні; [activeVin] — за яке
 * авто ведеться облік. Зазвичай це те саме, але не завжди: коли зв'язку немає,
 * водій може обрати авто вручну, щоб подивитися вже накопичене.
 */
data class Garage(
    val cars: List<CarProfile> = emptyList(),
    val activeVin: String = "",
    val detectedVin: String = "",

    /** Обмін даними авто між телефонами. Не зберігається: живе одну дію. */
    val share: ShareState = ShareState(),

    /** Чи прочитано список із диска. До того типові значення — не вибір користувача. */
    val loaded: Boolean = false,

    /**
     * Чим скінчилася остання спроба прочитати VIN.
     *
     * Не окраса. VIN читається один раз на підключення, і коли він не прочитався,
     * застосунок мовчки лишається при попередньому авто — саме так дані другої
     * машини одного разу лягли в теку першої. Без рядка в журналі це неможливо ані
     * помітити, ані пояснити.
     */
    val vinNote: String = "",

    /**
     * Чи підтверджено VIN-ом те авто, дані якого йдуть із шини ПРЯМО ЗАРАЗ.
     *
     * Знімається на кожному новому підключенні й ставиться назад тільки тоді, коли
     * машина сама себе назвала. Між цими двома митями застосунок бачить живі числа,
     * але НЕ ЗНАЄ, чиї вони, — і це не дрібниця.
     *
     * Ось як це коштувало ночі обліку. Телефон під'єднався до другої машини,
     * VIN не відповів, активним лишалося перше авто — і чужі лічильники пішли в
     * його теку. Заряд першого авто в цей час тривав: відкриту сесію збило,
     * базовий показ переїхав на чужі числа, а коли водій повернувся до своєї
     * машини, різниця виявилась неправдоподібною, і сесія почалася з нуля.
     *
     * Не зберігається: підтвердження живе рівно одне підключення.
     */
    val vinConfirmed: Boolean = false,

    /**
     * VIN промовчав, але живі пробіг і лічильники плавно продовжують активне авто —
     * отже це воно. Другий свідок особи, окрім VIN: без нього кілька авто в гаражі
     * плюс мовчазний VIN вимикали навчання й прогноз (див. [CarIdentity]).
     *
     * Не зберігається: підтвердження живе одне підключення, як і [vinConfirmed].
     */
    val vinConfirmedByContinuity: Boolean = false,

    /**
     * Людина сама сказала, у якій машині вона зараз, — коли ні VIN, ні неперервність
     * не впізнали авто. Свідоме підтвердження в банері «оберіть авто»: далі облік і
     * навчання йдуть у це авто, а його відбиток засівається на майбутнє.
     *
     * Не зберігається: підтвердження живе одне підключення, як і решта ознак особи.
     */
    val identityConfirmedManually: Boolean = false,

    /**
     * Питання «хто це?» ще відкрите: запит VIN надіслано, відповіді нема.
     *
     * Різниця між «ще питаємо» і «спитали й не почули» тут головна. Поки питаємо —
     * чекати правильно, це секунди. А от чекати ВІЧНО не можна: частина клонів
     * ELM327 не підтримує сервіс 09 взагалі, а частина авто віддає VIN лише при
     * увімкненому запалюванні — тобто саме на зарядці, коли авто вимкнене, VIN
     * може мовчати всю ніч. Зупинити на цій підставі облік зарядок означало б
     * втратити рівно те, заради чого цей облік і робився.
     *
     * Тому мовчання VIN не спиняє облік назавжди: далі за чужі числа відповідає
     * сторож пожиттєвих лічильників, який чужу батарею впізнає по стрибку.
     */
    val vinPending: Boolean = false,

    /**
     * Прохання перечитати VIN, не чекаючи наступного підключення.
     *
     * Ставиться, коли пожиттєві лічильники раптом упали: це або справді інше авто,
     * або заміна блока BMS, і в обох випадках питання «а хто це?» треба ставити
     * негайно, а не через годину.
     */
    val vinRecheck: Long = 0L,

    /**
     * Прохання видалити авто разом із його текою. Порожньо — нема чого видаляти.
     *
     * Дорученням, а не прямою дією: видалити теку — це файловий ввід-вивід, а
     * екран його не робить. Виконує блок гаража й одразу прибирає прохання.
     */
    val deleteVin: String = "",
) {
    val active: CarProfile
        get() = cars.firstOrNull { it.vin == activeVin } ?: CarProfile(vin = activeVin)

    /** Чи авто на шині — те саме, за яке ми рахуємо. */
    val mismatched: Boolean
        get() = detectedVin.isNotEmpty() && activeVin.isNotEmpty() && detectedVin != activeVin

    /**
     * Чи можна вести облік за числами, які йдуть із шини.
     *
     * Правило одне: або машина назвалася, або в гаражі просто нема з чим її
     * сплутати. Одне авто — і мовчання про VIN нічим не загрожує: інших тек немає.
     * Два й більше — мовчання означає «не знаю, чиї це числа», а здогад тут коштує
     * дорожче за хвилину пропущеного обліку.
     */
    val identified: Boolean
        get() = !mismatched &&
            (vinConfirmed || vinConfirmedByContinuity || identityConfirmedManually || cars.size <= 1)

    /**
     * Чи дивимось ми зараз НЕ на те авто, що на шині.
     *
     * Вибір руками — це перегляд, а не перемикання обліку. Людині буває треба
     * зазирнути в дані другої машини, сидячи в першій: порівняти комірки до
     * перепакування й після, глянути криву. Показати це можна, а записувати туди
     * живі числа з чужої шини — ні, і саме тому [identified] тут хибне.
     */
    val viewingOther: Boolean get() = mismatched
}

/** Що екран просить зробити з обміном. */
enum class ShareRequest { None, Export, Import }

/**
 * Обмін даними одного авто між телефонами.
 *
 * Одна машина, два водії, два телефони: кожен бачить свою половину поїздок, і жодна
 * з половин сама по собі не дає повної картини. Сервера в застосунку немає, а от
 * файл переслати вміє будь-хто.
 *
 * [exportedPath] — готовий файл, який екран має віддати системі «поділитися»;
 * [importPath] — файл, який людина обрала, і його треба прийняти. Обидва
 * порожніють одразу після того, як їх обробили: це доручення, а не стан.
 */
data class ShareState(
    val request: ShareRequest = ShareRequest.None,
    val exportedPath: String = "",
    val importPath: String = "",

    /** Що вийшло, людською мовою. Показується на екрані налаштувань. */
    val note: String = "",
)
